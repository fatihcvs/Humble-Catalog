# Plan

## Mode
Improvement. Auto-generated by /jeffy. To switch to Scoped mode, replace the Goal and Definition of done with a concrete target and seed BACKLOG.md with finite tasks. Scoped mode changes nothing else: the envelope, verify gate, checkpoint, and journal discipline still apply.

## Goal
Raise this project to the standard a top-tier engineer would sign off on: correct on edge cases, secure by default, tested where failure hurts, simple to change, honest in its docs. Audit dimensions: architecture, code quality, security, testing, error handling, performance, documentation, dependency hygiene, developer experience, correctness, observability, and, where a user-facing surface exists, UX and accessibility. Ordering principles: correctness and security outrank polish, simplicity outranks cleverness, deleting code beats adding it, and improvement is measured by user and maintainer impact, never by activity. Leave the project materially better on every pass.

## Operating envelope
Severity is judged against this envelope, never against imagined callers. Before filing any finding, the first audit enumerates every input surface - network endpoints, CLI arguments, environment, config files, data files, state files the project writes and reads back - and classifies each with one trust class, recorded in the table below. Later audits re-read the table and must not reclassify it.

Trust classes:
- adversarial: untrusted parties can reach it (public endpoints, uploads, third-party content). The full severity rubric applies, including hostile hand-crafted input.
- user-error: the project owner hand-authors it (CLI flags, config, seed data). A wrong value deserves a clear failure message; findings about exotic malformed shapes beyond that are Low at most.
- machine-generated: the project's own pipeline produces it. The generator's real output is the contract; hand-mangled or off-contract variants are out of envelope, Low at most, Declined by default.
- state-at-rest: the project writes it and reads it back. External corruption of it is out of envelope, Low at most.

Surfaces (first audit fills this in, one line each):
- CLI arguments and subcommand flags (`python -m humble_catalog ...`): user-error - the project owner types them; argparse plus the explicit `parser.error` calls in `__main__.py` are the clear-failure contract.
- Viewer HTTP API (Flask on 127.0.0.1, `humble_catalog/webapp/__init__.py`): user-error - reachable only from loopback, with the Host guard closing DNS rebinding and JSON content-type forcing a CORS preflight no cross-origin page gets past, so the callers are the viewer's own JS and the owner with curl. Not adversarial: no untrusted party can reach it.
- HumbleBundle order/bundle JSON and bundle pages (`extract.py`, `humble_api.py`, `parse_order.py`, `bundle_preview.py`): adversarial - third-party content the project does not control, reached over the network and parsed.
- External metadata API responses (`sources/*.py`, `url_import.py`): adversarial - third-party content, same reason; a compromised or merely changed upstream shape reaches the parsers.
- Cover image bytes fetched over the network (`covers.py`): adversarial - third-party bytes written to disk under a derived filename.
- Reference spreadsheets (`import-sheets`, .xlsx under `Reference spreadsheets/`): user-error - the owner hand-authors and hand-places them.
- Steam/Heroic library files on disk (`import_games.py`): machine-generated - written by those launchers; their real output is the contract.
- Environment variables (API keys, `HUMBLE_PORT`): user-error - the owner sets them.
- `catalog.db`, the harvest cache, and `covers/` read back by the project: state-at-rest - the project writes them and reads them back.
- Backup snapshots restored by `restore` (`backup.py`): state-at-rest - the project wrote them; a hand-mangled snapshot is out of envelope.

Binding rules:
- A finding exercised only by out-of-envelope input is Low at most, whatever its consequence.
- Only the user widens the envelope or reclassifies a surface. An audit that believes the envelope is wrong files one Proposed item and moves on; it never files the finding at rubric severity.
- Where robustness to malformed input on a surface is genuinely wanted, the remedy is one structural task - a single validation boundary where that input enters - never scattered per-site guards.

## Surface inventory
The audit's map, and the run's bound. Before filing any finding, the first audit enumerates the project's whole public surface - every public module, class group, entry point, or command a user can reach - as one checkbox row each, listed completely before any row is explored in depth. Enumerate from the code, not from the documentation: every source file belongs to some row, including underscore-private modules that implement public behavior, because dead or orphaned code that no entry point reaches is exactly what a call-path sweep never visits. Size rows so one checkbox certifies a comparable amount of surface: a module with dozens of public functions gets several rows split by function family, never one, or the rows-swept count overweights trivial rows and a single checkbox quietly stands for half the project. A row starts unswept: `- [ ] <surface>: <scope>`. An executed probe, and only an executed probe, flips it: `- [x] <surface>: swept at <commit> - <what the sweep exercised>`. A row whose implementing code changes after its recorded commit is stale: flip it back to unswept, because the old sweep no longer certifies the new code. A third state exists for surface this host cannot reach at all - a platform-only build, a service that cannot be installed here: `- [~] <surface>: unreachable on this host - <reason>`. It is a disclosure, not a sweep. Convergence requires no unswept `- [ ]` row, every `- [~]` row is named with its reason in the run report, and a later run on a host where the reason no longer holds flips it back to `- [ ]` and sweeps it.

For a surface that computes values, a probe is a correctness check, not a liveness check. Sweeping such a row requires at least one known-answer test per function family it covers - a closed form, a hand-computed small case, or an independent reference implementation - or a strong invariant that a wrong result would break. Run-without-crashing flips nothing there: the costliest defects return wrong numbers without complaint, and a crash-biased sweep files every type error while certifying wrong arithmetic as clean. A metric off by a constant factor, a parameter silently ignored, and an aggregation that quietly no-ops all pass a liveness probe and all fail a known-answer one. A row's known-answer battery is kept, not rebuilt: write it under `.jeffy/probes/<row-slug>` so the iteration checkpoints commit it, and a re-sweep re-runs that battery instead of reconstructing the instrument, which is where most re-sweep cost goes. A battery is updated in the same iteration as the behavior it pins, so it never certifies a contract the code no longer has.

A sweep also exercises every documented parameter of the functions it certifies, at two or more values that must change the output, including the negative or boundary side of the domain where one exists. A documented parameter whose value changes nothing is a finding, never a pass, unless the documentation itself says it should have no effect in that case. This is a recurring class, not a corner: an inert flag, a rate applied only when positive, and an aggregation token falling through to no-op are the same defect wearing three names, and a sweep that probes a parameter only where it works will certify the row while the parameter is dead everywhere else.

Dimension scores claim only swept rows. A None on a dimension whose surface still has unswept rows is silence, not cleanliness, and an audit that presents such a score as the whole project is the failure this table exists to prevent: a library scored correctness None across five audits while an entire simulation module sat unexamined, and every audit's new High was simply the next unvisited room. The first audit therefore probes breadth-first - every row shallowly before any row deeply - so the worst defect in the project appears in the first filing, not the sixth.

Convergence requires that this table lists no unswept row, and the Stop hook checks that mechanically. That is what makes the loop bounded: the surface is finite, sweeps only accumulate, and the remaining work is always the visible remainder of a written list rather than a hope that the next audit comes back empty. The run report states progress as rows swept of rows total.

Rows (first audit fills this in, one line each):

CLI and dispatch
- [x] cli-dispatch: swept at 082003f - `missing_dependencies`, `check_dependencies`, the argparse wiring and every explicit `parser.error` path; battery `.jeffy/probes/cli-dispatch/probe.py`, 62 cases through real subprocesses. The envelope makes CLI arguments user-error, where the failure MESSAGE is the contract, so each refusal is asserted on its text as well as its exit code. IMPORTANT for anyone extending this battery: it must never invoke a subcommand whose SUCCESS writes. `export`'s path is optional and defaults to catalog.csv, and an early version wrote two real catalog exports into the repo root; the happy path is now checked in-process against `export.COLUMNS` and from `--help`, and a standing guard case asserts no export file is left behind. 62/62.

Viewer HTTP API (`humble_catalog/webapp/__init__.py`, split by route family: `grep -n "@app\." humble_catalog/webapp/__init__.py`)
- [x] webapp-host-guard: re-swept at 78783eb after I1 changed webapp/__init__.py; the battery held 34/34 again. Previously re-swept at 5fe28bf after H3; the 34-case battery was re-run unchanged and held 34/34, as it did at 5c54d77 after H2. Originally swept at e5c665d - `host_is_loopback` and `refuse_foreign_hosts`; battery `.jeffy/probes/webapp-host-guard/probe.py` covers suffix/prefix spoofs, loopback-resolving addresses that are not loopback names, userinfo and fragment smuggling, absent and blank Host, casing and port variants, and that the 403 precedes routing on reads, writes, the static index and unknown routes. 34/34 held.
- [x] webapp-read-routes: re-swept at 78783eb after I1 changed webapp/__init__.py; the battery held 46/46 again. Previously re-swept at 5fe28bf after H3; the 46-case battery was re-run unchanged and held 46/46, as it did at 5c54d77 after H2. Originally swept at bbb46e2 - GET `/`, `/covers/<path>`, `/api/items`, `/api/stats`, `/api/keys`, `/api/review`, `/api/duplicates`, `/api/status`; battery `.jeffy/probes/webapp-read-routes/probe.py`, 46 cases against the real app through Flask's test client. `/api/stats` and `/api/keys` exist to RESHAPE a report the CLI also prints, and both document that the two must not be able to disagree, so the battery asserts the panel's payload equals `stats.report`/`keys.report` field for field rather than eyeballing the JSON. Also pins the documented absent-not-empty editions key, that a same-type duplicate is not an edition link, the review sort and filter, that `/api/duplicates` pops the raw hand_edited column rather than leaking it, that `/api/status` hides finished phases, six cover-path traversal shapes asserted on whether the outside file is ever SERVED, and that every read route survives an empty catalog. 46/46 held, no findings.
- [x] webapp-write-routes: re-swept at 78783eb after I1 changed webapp/__init__.py; the battery held 18/18 again. Previously re-swept at 5fe28bf after H3; the 18-case battery was re-run unchanged and held 18/18, as it did at 5c54d77 after H2. The `/comment` contract changed in H3 - the key is now required, so an empty or malformed body can no longer clear a note - and the battery's comment cases still hold because they send the key. Previously re-swept at b46399e after the A1 fix changed this code - POST rating/type/read-status/comment/user-tags/edit/revert/override/reopen/apply/choose; battery `.jeffy/probes/webapp-write-routes/probe.py` exercises malformed body, wrong value type, unknown item id, the rating domain's boundaries (0, 6, bool, null-to-clear) and the bare-body `/reopen` contract. 18/18 held.
- [x] webapp-tag-vocab-routes: swept at 78783eb - POST `/api/genres/{rename,delete}`, `/api/user-tags/{rename,delete,bulk}`, and the `tags` entry check on `/api/items/<id>/user-tags` that shares their boundary; battery `.jeffy/probes/webapp-tag-vocab-routes/probe.py`, 99 cases. These rewrite a VOCABULARY across every row that uses it, so each case asserts which rows moved, which did not, and the `changed` count the route reports - a rename that answered 200 and renamed nothing would pass a status check and fail these. Genre and user tags are asserted to be separate pools in every case, the documented titleize asymmetry is pinned at both values, `action` is exercised at add and remove, and bulk's 8 refusal shapes each assert the database is unchanged. Carries the I1 cases. 99/99 held, 55/99 against the code before the fix.
- [x] webapp-merge-routes: re-swept at 78783eb after I1 changed webapp/__init__.py, the battery holding unchanged; originally swept at 9739358 - POST `/api/merge`, `/api/dismiss_pair`, the type-mismatch refusal; battery `.jeffy/probes/webapp-merge-routes/probe.py`, 44 cases. These routes delete a row, so every refusal case reads the database back and asserts both rows survive and `merges` stays empty - a route that answered 400 while deleting anyway passes a status check and fails this. Covers the bool-is-an-int trap on both ids, the documented ebook-versus-audiobook refusal, that a dismissal keeps both rows where a merge drops one, that dismissing a pair in either direction stores one row, and end to end that a dismissed pair stops being offered by `/api/duplicates`. 44/44 held.
- [x] webapp-remote-routes: swept at c3e734f - POST `/api/items/<id>/fetch_url`, `/api/bundle-preview`; battery `.jeffy/probes/webapp-remote-routes/probe.py`, 40 cases. Scope is the ROUTE's contract - how each collaborator outcome maps onto a status and a body - because the network behaviour belongs to url-import, outbound-guard and the two bundle-preview rows, all swept. Collaborators are replaced at the seam rather than driven through a real server because neither route can reach a local one: url_import refuses a loopback address and fetch_bundle refuses a non-Humble host, so a real server would re-test the refusal and never reach the success mapping. Pins the link_only degradation in full (item's title, url kept, reason, host as source), the documented catch ORDER so a rejected url can never become an offer, that the two bundle-preview failure codes DIFFER, that neither route reaches its collaborator for an unusable url, and that a preview leaves the items table byte for byte unchanged. 40/40 held.
- [x] webapp-export-routes: re-swept at 78783eb after I1 changed webapp/__init__.py, the battery holding unchanged; originally swept at 9739358 - `/api/export.csv`, `/api/export.xlsx`, both GET and POST forms, `_export_request`; battery `.jeffy/probes/webapp-export-routes/probe.py`, 30 cases asserting CONTENT rather than status. Pins which rows come back for a given id list, that POST preserves the order the viewer sent (the filter lives in the browser, so re-sorting here would discard the owner's sort), that an empty id list is a real empty answer rather than the whole catalog, `columns` at two values that change the header, the documented asymmetry that an unknown column NAME is dropped while a non-list is a 400, the utf-8-sig BOM, and that the two formats agree on the rows and refuse a malformed body identically since `_export_request` is shared. 30/30 held.

Storage layer (`humble_catalog/db.py`, 683 lines, split by function family: `grep -n "^def " humble_catalog/db.py`)
- [x] db-schema: swept at d474518 - `connect`, the schema it applies, and every step of `_migrate`; battery `.jeffy/probes/db-schema/probe.py`, 50 cases. Each case BUILDS a legacy database - old schema, old data shapes, no user_version - and drives it forward, because a fresh database takes every migration as a no-op and would exercise almost none of this code. Covers the added columns, the tag-array and genre-case rewrites, hand_edited derived from pre_edit, the external_keys re-key, connect() idempotence across three opens, and the documented json_valid guard: a row whose `raw` will not parse is skipped and the database still opens, including when EVERY row is unparseable. 50/50 held, no findings.
- [x] db-tags: swept at e5c665d - `tags_to_json`, `tags_from_json`, `titleize`, `normalize_tags`, `rename_tag`, `delete_tag`, `bulk_user_tag`; battery `.jeffy/probes/db-tags/probe.py` exercises `col` at both values (GENRE titleizes, USER_TAGS does not) and `action` at both, plus the pre_edit snapshot rewrite, NULL-on-empty, and vocabulary case-snapping. 42/42 held.
- [x] db-items: swept at e5c665d - `fetch_items`, `apply_hand_edit`, `merge_items`; battery `.jeffy/probes/db-items/probe.py` pins snapshot-once (a second edit must not re-snapshot), merge refusals deleting nothing, fill-if-empty versus the user_tags union, and the `merges` tombstone. 25/25 held.

Acquisition (HumbleBundle)
- [x] extract-humble: re-swept at 9037db1 after the E1 fix changed this code; the battery grew to 39 cases and holds 39/39, up from 29/33. Originally swept at 3ea3704 - `humble_catalog/extract.py`, `humble_catalog/humble_api.py`; battery `.jeffy/probes/extract-humble/probe.py`, 33 cases against a real `http.server` on an ephemeral port with `BASE` repointed, never a mocked session. Exercises the logged-in contract at five failing values (html body, redirect, refusal, server error, absent Content-Type) each of which must flip the answer, the `all_tpkds=true` parameter with the server's own request log as the assertion, the throttle by counting sleeps at two `delay` values, `refetch` at both values, and the cover_path contract. 29/33 held; the 4 failures are E1. Scope excludes the playwright login helpers, which are their own row below rather than being credited to this one.
- [ ] humble-login: `humble_api.get_cookies`, `manual_login`, `ensure_login` - the playwright profile, the non-automated login subprocess, and the retry-then-SystemExit path. Split out of extract-humble because a sweep needs a real browser and a profile directory; reachable on this host, just not covered by the battery that swept the client. Enumerate with `grep -n "profile_dir" humble_catalog/humble_api.py`.
- [x] parse-order: swept at e0b77ba - `parse_order`; battery `.jeffy/probes/sources-base/probe.py` covers platform filtering (both skip branches), format lowercasing and sorting, a missing payee, the gamekey in the bundle url, external key extraction, and an order with neither subproducts nor tpkd_dict. Held.
- [x] bundle-preview-parts: re-swept at e662825 after H1 changed `fetch_bundle` and `delivery_stores`; the 26-case battery was re-run unchanged and held 26/26, and the tiers battery added the refusal cases for a foreign and a lookalike host so the gate is pinned on both sides of the change. Originally swept at cfb97ff - `fetch_bundle`'s scheme and host gates (refusal paths, no network), `delivery_stores`, `_adds`, `_owned`, `_owned_games`; battery `.jeffy/probes/bundle-preview-parts/probe.py`. Held.
- [x] bundle-preview-tiers: swept at e662825 - `preview()`, `_overlaps`, `_series_note`, `format_report`; battery `.jeffy/probes/bundle-preview-tiers/probe.py`, 57 known-answer cases against a seeded catalog and hand-built bundle payloads. The counting rules a purchase decision rests on are asserted as numbers the report MUST show, not as absence of complaint: owned versus new, the merges union, price ordering, `adds` disjoint and summing to the richest tier's new count, cumulative-tier dedupe, the game verdict routing across all three bands, keyed-versus-activated precedence, unimported-store scoping to unmatched items only, and series hits leaving `overlaps`. `format_report`'s `encoding` is exercised at two values that must change the output (utf-8 keeps the symbol, cp437 falls back to the ISO code and never to a replacement character) and `preview`'s `url` at two. Also carries the H1 off-shape cases. 57/57 held, 41/55 against the code before the fix.

Title and match logic
- [x] matching-score: swept at 70adab5 - `matching.score`/`status_for`; battery `.jeffy/probes/pure-scoring/probe.py`, closed-form weights and both threshold boundaries, every parameter varied. 26/26 held.
- [x] classify-type: swept at 70adab5 - `classify.classify`; same battery, all four documented parameters varied including the negative "bare audio substring" side. Held.
- [x] titles-clean: re-swept at b3d8ad7 after the F1 fix changed sequel_mismatch; the battery grew to 84 cases and holds 84/84, up from 83/84. Originally swept at cfe9b54 - `clean_title`, `clean_game_title`, `sequel_mismatch`, `sort_tokens`; battery `.jeffy/probes/titles/probe.py`, which covers both rows. Exercises each stripping rule with its NEGATIVE side (a novel/edition/audio word that is not trailing must survive), the compact and ordinal edition spellings, the stacked game-suffix loop, that a trailing numeral is never stripped, and that only the last trailing parenthetical goes. The sequel rule is exercised on digit pairs, roman pairs, identical, unrelated and word-suffixed titles. 1 failure, F1.
- [x] titles-series: swept at cfe9b54 - `series_key`, `parse_series` and the marker family; same battery. Exercises 7 volume spellings, 4 range forms including a REVERSED range that must normalize low-to-high and a single-volume range, 7 collection words, the subtitle-discarding rule stated as the property that two subtitles of one volume are equal, `number_hint` at two values plus the case where a marker must override it, the punctuation-merging of three initialism spellings against the negative pair that must stay apart, and that display keeps punctuation while key discards it. No findings.
- [x] series-report: swept at 70adab5 - `series.collapse` and `series.sort_key`; pure-scoring battery covers the docstring example, the gap case, unsorted and duplicate input, the empty case, and both ordering ranks. Held.
- [x] series-db: swept at 5d5642f - `series.owned_volumes`, `series.describe` against a seeded database; battery `.jeffy/probes/series-db/probe.py`, 15 cases. Asserts the re-buy outcome directly (an offered volume already held is flagged, a continuation is not), that a gap renders as `Vol. 1-2, 5` rather than smoothing into a run the owner does not hold, that only an explicit range carries a span while a single volume invents none, and that two spellings of one volume index to a single number so a duplicate cannot inflate the count. 15/15 held.
- [x] editions-dedupe: swept at d474518 - `dedupe.dedupe_key`/`find_groups`, `editions.edition_key`/`find_groups`; battery `.jeffy/probes/editions-dedupe/probe.py`, 54 cases. Both modules reject fuzzy matching as measurably LESS accurate, so the negatives are probed as hard as the positives: the one-word subset trap that scored 100 under fuzzy stays apart, `#` and `+` keep C, C# and C++ distinct, a leading format word is kept while a trailing one strips, a marker-only title never strips to the empty key, and android/music stay excluded from edition grouping. Also pins the per-pair dismissal rule that a naive filter gets wrong - a member dismissed against one sibling but not another stays in the group. 54/54 held, no findings.
- [x] game-match: swept at 5d5642f - `prepare_pool`, `classify_game`, GAME_OWNED and GAME_POSSIBLE; battery `.jeffy/probes/game-match/probe.py`, 25 cases. Both cutoffs are pinned on both sides with DERIVED answers: `fuzz.ratio` is 2*M/T, so synthetic letter pairs score 100.00, 92.31, 90.00, 83.33 and 40.00 by hand, and the two constants are themselves asserted so the derivation fails loudly if either moves. Also pins the index-parallel invariant by querying a three-title pool for each title in turn, the sequel rule in both directions, the F1 digit-versus-roman case, an edition suffix matching the base game, the empty pool and empty-normalizing title, that the display title rather than the sorted key is returned, and the documented refusal of a plain list. 25/25 held.

Enrichment and harvest
- [x] enrich-core: swept at 2359d10 - `enrich.run`, `enrich.reset`, `apply_candidate`, `series_from_title`, `_winning_candidate` and the EDITABLE_FIELDS contract; battery `.jeffy/probes/enrich-core/probe.py`, 64 cases driven with stub sources so the decision logic rather than the network decides the outcome. The rules that protect the owner's typed values are asserted as the properties they claim: an overridden row that finds no confident match keeps its status, its value and its hand-edited flag while the override flag clears anyway; a confident match overwrites but snapshots first; `reset` at both `reviews_only` values, sparing hand-edited rows at one and not the other, and never touching the items table so a rating and a type override survive; and the snapshot's keys are asserted to BE EDITABLE_FIELDS, so a field added to the UPDATE without being added to that list would fail rather than silently stop being revertible. 64/64 held, no findings.
- [x] enrich-topups: re-swept at b88a9cd after G1 removed the dead guard; the 36-case battery was re-run unchanged and held 36/36. Originally swept at 5cfe0e0 - `enrich.credits`, `enrich.fill_series`, `enrich.override_edited`; battery `.jeffy/probes/enrich-topups/probe.py`, 36 cases. Each pass amends rows enrich.run has finished with, so the cases assert what is NOT touched: `fill_series` owns two columns and leaves status, hand_edited and match_confidence alone, its COALESCE keeps a typed series name while adding only the missing number, and it is idempotent, which is the documented recovery path after a reset; `credits` skips a comic it has already filled so a re-run asks nothing, skips the unmatched, the non-comic and the candidate with no first-issue url, and survives one source failure without losing the pass; `override_edited` refuses a near-miss confirmation case-sensitively and queues nothing. 36/36 held. One finding, G1, from reading rather than from a failing case.
- [x] harvest-run: swept at f26460d - `build_worklist`, `_is_429`, `_run_pool`, `_repeat_lines`, `_when` and `run`; battery `.jeffy/probes/harvest-run/probe.py`, 58 cases driven through the `sources=`/`_conn=` seams so the real threading and tallying run with no network. The weight is on four failure modes with four different documented responses, each asserted on what it does AND what it must not do: a CacheMiss is not a failure and records nothing; an ordinary error records a row and keeps draining, checked by asserting the titles below it are still asked for; a 429 records NO failure row but does take the source offline and record the quota, and keeps serving the rest from cache; a SECOND 429 breaks the walk. Redaction is asserted on both consumers, the log line and the stored row. The worklist's determinism rules are pinned as documented, including the case-only tie broken by the raw string. `ignore_quota` is exercised at both values and an expired record is asserted not to be believed. 58/58 held.
- [x] harvest-reports: swept at 25ac5ec - `runs.record`/`history`/`forget` and `harvest.report_runs`/`report_failures`/`forget_runs`; battery `.jeffy/probes/harvest-reports/probe.py`, 28 cases. The trim is counted in RUNS not rows - asserted with four runs of three sources at keep=2, where the two newest survive WHOLE at six rows - because trimming rows would behead a run mid-way and leave a partial record reading as a complete one. The missing denominator is pinned on both sides: a cache-only source prints a dash, never 0%, since 0% would claim it never fails when it never tried. The PRIVACY contract is asserted directly - a failure row carrying a title is seeded and the runs report is checked NOT to contain it, which is what makes that output safe to paste. 28/28.
- [x] quota-gate: swept at 5d5642f - `next_pacific_midnight`, `record`, `blocked`, `clear`, `hit_at`; battery `.jeffy/probes/quota-gate/probe.py`, 23 cases. PACIFIC is a fixed offset, so every expected instant is written out in full rather than recomputed by repeating the implementation. Pins the strictly-after boundary at the exact instant and one second either side, the same instant spelled in a third zone, and an invariant over 48 hourly inputs that every answer is strictly ahead and within 24 hours - which a constant return or an off-by-one-day breaks. `blocked`'s `now` is exercised at two values that flip the verdict. The two commit contracts are asserted directly: `record` commits, checked from a second connection; `clear` does not, checked by rolling back and seeing the row return. The fixed UTC-8 offset is pinned as the documented DST trade, not filed. 23/23 held.
- [x] progress-failures: swept at a4462c9 - `duration`, `_column_count`, `grid`, and `failures.record`/`top`/`error_kind`/`count_since`; battery `.jeffy/probes/progress-failures/probe.py`, 49 cases. `duration` is pinned at all three documented shapes, at both band boundaries in each direction, at its own docstring examples, and on truncating rather than rounding so a progress line never overstates elapsed time. `_column_count`'s even-grid preference is asserted at the case its docstring describes, and the `max(1, ...)` guard directly, since 0 columns would divide by zero in `grid`. `error_kind` is asserted as the grouping property it exists for. The failures table's identity rules are pinned - increment not duplicate, first_failed_at kept while last moves, one title at two sources being two rows - with `min_failures` at three values and `count_since` counting TITLES, scoped to one source, inclusive at the boundary. 49/49 held.

Metadata sources (`humble_catalog/sources/`)
- [x] sources-base: swept at e0b77ba - `redact`, `candidate`, `cache_key_params`, `_with_retries`; battery `.jeffy/probes/sources-base/probe.py` counts attempts and sleeps rather than watching a call succeed, exercises `retry_server_errors` at both values including that it does not disable connection retries, and pins that `candidate`'s `extra` is a fresh dict per call. Held.
- [x] sources-accessors: re-swept at 9037db1 after E1 moved this code to humble_catalog/shapes.py, where the row's scope now lives; the 75-case battery was re-run unchanged and held 75/75. Originally swept at 3ea3704 - the shape boundary added by D1 in `humble_catalog/sources/base.py`; battery `.jeffy/probes/sources-accessors/probe.py`, 75 cases. Exercises `as_number`'s `allow_text` and `text_list`'s `key` at two values each and asserts each parameter changes the answer, both bool values against the int-subclass trap, the unwrapped-string case for `first_text` and `first_mapping`, and the property the boundary exists for: 133 calls across 19 shapes and 7 functions raise nothing. 75/75 held. `as_text` deliberately preserves surrounding space - trimming belongs to `db.normalize_tags`, which does it at db.py:161 - and that was checked rather than assumed before being asserted.
- [x] sources-books: re-swept at 9037db1 after E1 moved the accessors to humble_catalog/shapes.py; the 61-case battery was re-run unchanged against the re-pointed imports and held 61/61. Previously re-swept at 3ba6109 after the D1 fix changed this code - `google_books.py`, `open_library.py`, `hardcover.py`; the 61-case battery was re-run against the changed parsers and now holds 61/61, up from 54/61. Originally swept at f0a257d; battery `.jeffy/probes/sources-books/probe.py`, 61 known-answer cases exercising the url fallback chain at three values, the api key and the token at two values each (including that a pasted Bearer prefix is stripped and that rotating a key still hits the cache, since the credential is kept out of the cache key), the featured_series versus series_names precedence at both values, and the GraphQL error payload that must raise before anything is cached. 54/61 held; the 7 failures are all D1.
- [x] sources-media: re-swept at 9037db1 after the same move; the 82-case battery was re-run unchanged and held 82/82. Previously re-swept at 331077b after the D2 fix changed `oreilly.py`; the 82-case battery was re-run and now holds 82/82, the SCALE group included. Previously re-swept at 3ba6109 after the D1 fix, at 77/82 with 0 DRIFT failures. Originally swept at f0a257d; battery `.jeffy/probes/sources-media/probe.py`, 82 known-answer cases exercising the web_url form at three values, the rating scale at both sides of its boundary, the sequence parse on its numeric, decimal, non-numeric and absent values, every role atom of the narrow illustrator policy including the documented cover-only omission, and credits' three refusal paths with the request log as the assertion. 69/82 held; the 13 failures are D1 and D2. Records one gap rather than implying coverage: credits' ACCEPTING path is not exercised, because an allowed host falls through to a routability test that resolves the name, and a probe that depends on live DNS is one the network decides.
- [x] outbound-guard: re-swept at 0a5816b after C2 added the body cap; battery grew to 49 cases and held 49/49, its C2 end-to-end cases scoring 45/49 against the uncapped downloader. Originally swept at 45a18f4 - `humble_catalog/outbound.py`, the single validation boundary for a URL named by third-party content; battery `.jeffy/probes/outbound-guard/probe.py` exercises `allowed_hosts` at two values on one routable URL that must flip verdict, `check_initial` at both values against a real server whose request log is the assertion, 6 refused schemes, 7 private address shapes, and both C1 reproductions. 41/41 held, and 34/41 against the unfixed code.
- [x] url-import: re-swept at 0a5816b after C2 moved its capped read into outbound too; the 46-case battery held 46/46 again. Previously re-swept at 45a18f4 after C1 moved its destination rules into `humble_catalog/outbound.py`; the 46-case battery was re-run unchanged against the refactored code and held 46/46. Previously swept at 2a31136 after the B2 fix changed this code - `normalize_url`, `host_of`, `resolve` routing, `_fetch_html`, `_publicly_routable`, `_check_redirect_target`; battery `.jeffy/probes/url-import/probe.py` covers 9 rejected schemes in both casings plus whitespace/control obfuscations, lookalike-host routing, a two-real-server redirect reproduction now refused, the address predicate on both sides, and the host:port shape against the scheme gate. 46/46 held.
- [x] covers-store: swept at 4ac8be3 - `cover_filename`, `relink`, `store_order`; battery `.jeffy/probes/covers-store/probe.py` pins the exact derived basename for a fixed key, that eight separator and traversal shapes all reduce to a bare basename in the allowed character set, that three casings share one slug and stay distinct by digest, `relink`'s covers_dir at two values, and store_order's idempotence, merge tombstone, type precedence, override and post-reset snapshot rules. 57/57 held.
- [x] check-cmd: swept at c3e734f - `run`, `_board`, the OK/SKIP/FAIL verdicts and the summary; battery `.jeffy/probes/check-cmd/probe.py`, 34 cases driven through the module's own `sources=` injection seam, so every branch is reached with no live request. The case the row is worth sweeping for is REDACTION: an error carrying an API key is asserted not to survive into the returned detail nor into anything written to the stream, while the 401 that tells a bad key from a dead host does survive. SKIP is asserted to be a refusal to ASK rather than a label, using a stub that records every call. The summary is checked as its exact sentence and as an invariant that the three counts partition the sources. 34/34 held.

Import, export, lifecycle
- [x] export-columns: swept at c5b285b - `_columns`, `_select`, `_row`, `_clean`, `write_csv`, `write_xlsx`; battery `.jeffy/probes/export-stats/probe.py` exercises ids and columns at two values each, canonical column order against caller-owned row order, earliest-purchase selection, the empty-bundle case, and the illegal-character set against the legal whitespace it must keep. Held.
- [x] import-sheets: swept at 6ecca3f - the header vocabulary, `_cell`, `norm_title`, `type_scope`, `build_index`, `match` and `import_row`; battery `.jeffy/probes/import-sheets/probe.py`, 62 cases. The gap-fill rule is asserted directly: a row whose fields the owner has already set is read back UNCHANGED after an import that had something to say about all of them. Pins the distinction `unknown_headers` exists for - a header mapping to None is understood and dropped, an unrecognised one is a typo that imports nothing, and only the second is reported - that a numeric 0 survives `_cell` because a 0 rating means unrated, that an integral float normalizes to its integer so a numeric title matches at all, and that a duplicate title answers `ambiguous` rather than guessing. 62/62 held.
- [x] import-games: swept at 6ecca3f - `read_heroic`, `fetch_steam`, `store_games`, `imported_stores`; battery `.jeffy/probes/import-games/probe.py`, 35 cases from fixtures through the `root=` and `http=` seams, no network. The weight is on two REFUSALS, both existing because an empty list is written by two normal states (a logged-out Heroic store, a private Steam profile) and acting on it would delete a good library: `store_games` raises on empty and `read_heroic` raises on unparseable JSON, each asserted by reading the games table back and comparing it to what it held before. The transaction guarantee is exercised - a malformed row raises after the delete has run and the previous rows are still there. Per-store replacement is pinned on both sides. 35/35 held.
- [x] keys-report: swept at cdff75b - `store_for`, `parse_expiry`, `report`'s four-state machine and its three-group ordering, `stale_hides`, `missing_keys`, and `format_report`'s `show_all`/`hidden`; battery `.jeffy/probes/keys-report/probe.py`, 60 cases against an injectable clock so every expiry answer is fixed arithmetic rather than wall-clock drift. The ordering is written out in full: dated rows split AROUND the undated ones (live soonest first, then undated, then expired most-recent first), because plain ascending sorts dead keys above the ones still savable. Pins the uncheckable-vs-unredeemed boundary on both sides including the imported-but-empty store, the count partition as an invariant, that `expiring` excludes hidden AND expired rows, that a hide on a now-matched key is NOT stale (the case that justifies its separate query), and that an unreadable blob still reports. `show_all` and `hidden` each exercised at both values. 60/60 held.
- [x] stats-report: swept at c5b285b - `_tally`, `_by_type`, `_by_rating`, `_by_read_status`, `_by_enrichment`, `_by_gap`, `_by_genre`, `report`, `console_safe`; same battery, with the `default` and `encoding` parameters each exercised at two values, an out-of-vocabulary value asserted to be counted nowhere, and the section-need-not-sum-to-total property checked directly. Held.
- [x] backup-restore: swept at 8d8d59f - snapshot naming, the cover zip, `_items`, `_when`, `_safety_copy` and `restore`'s confirmation; battery `.jeffy/probes/backup-restore/probe.py`, 73 cases. Weighted towards what must NOT happen: every refusal path compares the live catalog's BYTES before and after, which is stronger than checking the return value. The confirmation is driven with eight wrong answers plus EOF; validation is proved to precede the prompt by making the confirmation raise if reached. The WAL case is exercised rather than assumed - a row committed through a live connection is asserted to be in the snapshot, which a plain file copy would miss. Sidecar removal, the safety copy holding the REPLACED rows, the damaged-catalog raw copy keeping the bytes verbatim while not blocking the restore, and zip traversal landing every entry inside covers/ are all pinned. 73/73 held.
- [x] reset-cmd: swept at 082003f - `DERIVED_TABLES`, `run`, the snapshot and the confirmation; battery `.jeffy/probes/reset-cmd/probe.py`, 42 cases. Asserts what SURVIVES: raw_orders and source_cache are intact after a wipe, being what a rebuild reads FROM. The snapshot's DELETE-first rule is exercised end to end - reset with a rating, rebuild with it cleared, reset again, assert no stale snapshot - because otherwise the old value resurrects on the next rebuild. The delete ORDER is asserted both functionally, under `PRAGMA foreign_keys = ON`, and structurally, every child table preceding `items` with `bundles` last. The three preserved tables are asserted ABSENT from DERIVED_TABLES. Seven wrong confirmations and EOF each leave every table untouched. 42/42.

Viewer front end (`humble_catalog/webapp/static/`, `ls humble_catalog/webapp/static`)
- [x] js-catalog-render: re-swept at a5098e5 after the same harness change, holding 48/48 unchanged. Originally swept at 2e5d715 - `render()`, `highlight`, `statusSelect`, `stars`, `shownRows` and the shared `esc`/`tagBadges` helpers, run for real in node; battery `.jeffy/probes/js-catalog-render/probe.py`, 48 cases. Escaping is asserted as exact strings and by stripping the one element `highlight` may introduce, so no other `<` may survive any of its three slices; the span-overlap rule is pinned because getting it wrong duplicates text rather than raising; each render guard is driven with the field it exists for (`tagBadges(undefined)`, `person` on a row with neither name field, `esc(null)` and `esc(undefined)`, a row with no status); and the count line is asserted exactly at 3/3, 1/3 filtered, the relevance suffix, and 0/0 empty. A row with no cover is asserted to emit NO img rather than one with an empty src. 48/48 held. Recorded, not filed: the `narrator` and `bundle` chipFilter accessors spread their fields unguarded and a row omitting them throws, but `db.fetch_items` cannot emit that shape, so it is out of envelope as machine-generated.
- [x] js-catalog-filter: re-swept at a5098e5 after the same harness change, holding 41/41 unchanged. Originally swept at af9c881 - `visible()`, `passesChipFilters`, `chipFilters`, `sortValue`, run for real in node through `tests/js_harness.py`; battery `.jeffy/probes/js-catalog-filter/probe.py`, 41 cases in ONE node process. Every case states the exact id list that must come back, in order, because a filter keeping too much still renders a plausible page and a sort falling back to insertion order still looks sorted. All eleven `flag` values are pinned to their exact row sets; `review` is asserted to EQUAL unmatched plus low_confidence with each named flag strictly narrower; chip `mode` is exercised at both values on one chip set and changes the answer; the narrator chip is asserted to span illustrator; `read_status` is asserted to sort by lifecycle ordinal rather than alphabetically; a missing status is checked on both the filter and the sort; and filters are asserted to combine as AND. 41/41 held.
- [x] js-fuzzy: re-swept at a5098e5 after the harness element stub began recording attributes; the 28-case battery was re-run unchanged and held 28/28. Originally swept at a4462c9 - `fuzzy.js`, run for real in node v24 through `tests/js_harness.py`; battery `.jeffy/probes/js-fuzzy/probe.py`, 28 cases in ONE node process. Every score is DERIVED from the formulas the file documents rather than recorded: T1 `0.90+0.10*len(q)/len(t)`, T2 `0.45+0.40*matched/tokens`, T3 `0.40+0.10*min(1,chars/words)`. An identical query scores 1.00, a 7-of-16 fragment 0.944, a 4-of-16 fragment 0.925, reordered whole tokens 0.85, full initials 0.50 - all exact. Also pins the ordering BETWEEN bands, AND semantics on an unmatched query token, the invariant that no score falls between 0 and the 0.40 cutoff, and that spans index the ORIGINAL string rather than the folded one, including across an elided apostrophe. 28/28 held.
- [ ] js-autocomplete: `autocomplete.js`.
- [x] js-panels: swept at a5098e5 - `KEY_STATES`, `displayState`, `shownKeys`, `keyChipCounts`, `keysExpiring`, `money`; battery `.jeffy/probes/js-panels/probe.py`, 26 cases. The row's weight is a counting INVARIANT the code documents as the fix for a real bug and calls true 'by construction': for all four chips, the count is asserted to EQUAL the length of what selecting that chip shows, and the four sum to every row. Reading the server's `counts` instead would make a chip promise more rows than clicking it delivers, since that map includes matched and hidden keys. `displayState`'s override is pinned on two rows with different server states, both collapsing to hidden; `keysExpiring` excludes the hidden and the expired. 26/26.
- [x] js-shell: swept at a5098e5 - `SECTIONS`, `currentSection`, `showSection`, `badgeCount`, `renderBadges`; battery `.jeffy/probes/js-shell/probe.py`, 27 cases in node. Pins the documented refusal to be helpful: an unknown hash falls back to Library WITHOUT rewriting the URL, asserted by reading `location.hash` back and requiring it untouched, because a silent rewrite would erase the evidence that a bookmark went stale. The hash form is exact in both directions (`#library` and `#/LIBRARY` both fall back). `showSection` is pinned on panel visibility AND aria-current, with an invariant that exactly one tab is current - two claiming `page`, or none, would render plausibly and mislead assistive tech. A zero badge renders empty, and the library's badge stays empty even when a pending count is set for it. 27/27.
- [x] viewer-markup: swept at 36b884c - `index.html` and `style.css`; battery `.jeffy/probes/viewer-markup/probe.py`, 40 cases, read as text with no harness. The cases that earn the row are CROSS-FILE: every `$("#id")` and `querySelector("#id")` in the viewer's scripts is asserted to exist in the markup, because a stale selector answers null and writes nowhere while every script-only test still passes; and the section list is READ from shell.js so a section added to one file and not the other fails here, with a tab, a panel, a badge slot, a route href and a hidden panel asserted for each. A guard case asserts the selector scan found more than ten selectors, so the check cannot pass vacuously. CSS: the active chips are marked by outline and weight rather than hue alone, and no `var(--x)` is used without being defined. 39/40 - the one failure is J1.

Scripts and gates (`ls scripts`)
- [x] privacy-gates: swept at 25ac5ec - `should_scan`, the scanner sharing, and `check_no_data_tracked`'s pattern list; battery `.jeffy/probes/privacy-gates/probe.py`, 59 cases. Aimed at COVERAGE rather than matching, because a gate that scans too little reports `clean` exactly as loudly as one that scans everything. Pins the nine excluded directories at any depth AND that a directory merely containing an excluded word is still scanned; the whole-filename rule for `.db-wal` and its siblings; that neither scanner reads leak_check.py, whose old blobs quote since-removed ALLOWED entries; that both scanners share one matcher and one should_scan, asserted on `__module__`; and 14 forbidden data shapes against 8 innocent repo files. Deliberately does NOT repeat the 27 word-boundary tests in tests/test_leak_check.py. 59/59.
- [ ] demo-catalog: `scripts/demo_catalog.py` - the invented-title viewer used for screenshots.
- [ ] favicon-fixtures: `scripts/make_favicon.py`, `scripts/capture_*_fixture.py`.
- [ ] os-wrappers: `scripts/{windows,macos,linux}/*` - setup, serve, stop, test, verify, leak-check parity across the three.
- [ ] packaging-ci: `pyproject.toml`, `.github/workflows/ci.yml`, `.gitattributes`.

## Verify command
One runnable command that must exit 0 for this project to count as unbroken - the test suite, the build, or a validator script. The first audit fills this in; write none with a one-line reason only if the project genuinely has no runnable gate. It runs at the end of every iteration: a failure the previous checkpoint did not have means the iteration broke the project, and the iteration is reverted.

Command: `.venv/Scripts/python.exe -m pytest -q && .venv/Scripts/python.exe scripts/check_no_data_tracked.py && .venv/Scripts/python.exe scripts/leak_check.py`

That is exactly what `scripts/windows/verify.ps1` runs, spelled so bash can execute it on this host: the project's test suite, then the two privacy gates the owner's standing order requires. It is written as the three steps rather than as a call to the .ps1 wrapper because the wrapper needs PowerShell and reports failure through `Write-Error`, whose exit status depends on `$ErrorActionPreference`; `&&` between the three makes the failure status unambiguous. Takes roughly 90 seconds.

## Method
Audit the project against every applicable dimension and score each by its highest finding severity: High, Medium, Low, or None. Skip dimensions that do not apply and record why. Every audit consults the Surface inventory first and sweeps unswept or stale rows before re-probing swept ones; an audit never wanders. When replenishing with a partial audit, target the dimensions least recently scored and the inventory rows least recently swept.

Severity rubric - judge by consequence, not by effort to fix:
- High: users get wrong results, data loss or corruption, a security vulnerability, a crash on realistic in-envelope input, or a broken build or install.
- Medium: failure on a plausible in-envelope edge case, a silently swallowed error, missing tests on a path whose failure would matter, misleading documentation, or a dependency with a known vulnerability.
- Low: polish - naming, style, minor docs, cosmetic gaps.

Evidence rule: a finding exists only if you can point at it - a file and line, a failing command, or a reproduced behavior. Verify before filing; no speculative findings. File the root cause, not each symptom.

Publication rule: code that leaves this project - an issue body, a pull request description, a report, a message to anyone - must be run in exactly the form it is published, and its stated output must be that run's real output. Trimming a working script down to something shorter and clearer does not inherit the original's verification: a reduction is new code, and it is unverified until it has been executed as written. Verifying one artifact and shipping a different one is the failure this rule exists to stop, and it is expensive, because the person who finds the broken version is the person whose trust was being asked for.

Write findings into BACKLOG.md as prioritized checkable tasks, worst severity first: High in Now, Medium in Next, Low in Later; break ties by user impact, then by smallest effort. Each line names its class: `- [ ] <ID> (<Severity>, <class>, <dimension>): <finding>. Acceptance: <check>.` - class is one of runtime, test, build-ci, docs, dev-tooling, chosen by the files the fix will touch, and a legacy line without one is read as runtime. Order within a section by severity first, then runtime before the other classes, because shipped behavior is what a user meets and perimeter work is what a run drifts into. Every task must be completable in a single iteration - split it if not - and must carry an acceptance check that is a runnable command or an observable fact, never an opinion. An acceptance check must be strong enough to fail: run it against the unfixed code and confirm that it does, because a check the broken implementation also satisfies proves nothing. Prefer a differential or invariant assertion over an absence-of-badness one - a sweep asserting only that output holds no NaN and no infinity is satisfied by a function that returns a constant, and a green suite, full line coverage and that sweep all passed at once over exactly that defect. For a user-facing surface, the check must exercise the running surface end to end - drive the UI (browser automation where available) or invoke the real CLI; a unit test alone does not close a user-facing task. A finding judged not worth fixing goes under Declined with a one-line reason; never re-file a Declined item. Not worth fixing is priced, not felt: a Low whose class is not runtime and whose fix plus its regression test cannot be completed inside one iteration is by policy not worth fixing, so move it to Declined with the reason `cost: exceeds one iteration` and name it by ID in the run report; that pricing outranks the splitting rule above, because splitting such a Low into two one-iteration tasks is exactly how the price gets evaded. That is a Declined reason and never a severity change; severity is set by the rubric and the envelope alone, and downgrading a finding to reach convergence is a violation.

Class rules: when a finding is one instance of a repeatable idiom - a type guard, a parse, an escape, an error path - file and fix the class, not the instance. The fix's acceptance check must include an enumeration, a grep or AST listing of every site where the idiom appears, and every site must be covered or explicitly settled. Record each closed or declined class as one line under Settled classes in BACKLOG.md. Three-strike rule: the third finding sharing one root cause with two earlier findings ends instance work; replace it with either one structural task that closes the class at a boundary or one Proposed envelope decision, and never patch a fourth instance.

Working rules, each of them learned by losing an iteration to it: an inventory row's scope line names the enumeration command that defines it - the grep, the listing, the glob - never prose, because a scope nobody can re-run is a scope nobody can check; a removal task greps the docs for the removed names in the same iteration; a fix iteration runs the filed reproduction as its first command, because backlog lines are hypotheses that rot as sibling fixes land around them; and a hash written into a state file is copied from rev-parse output, never typed.

Closeout: a run that has produced one full fresh-evidence audit scoring zero High and zero Medium in-envelope stops auditing for the rest of the run, works or declines whatever is already on the ledger, and converges. Without that stop a project with a long tail of Low findings never finishes: the ledger drains, the drain triggers a replenishing audit, the audit files another Low, and the loop is productive forever without ever being done. The stop is safe because reaching it requires an audit that just came back clean of High and Medium; a High or Medium found while executing a task is still filed and still worked, and both the evaluator gate and the next run's fresh audit remain in force.

Audit discipline: the audit procedure is exactly what this Method prescribes. Never escalate rigor unilaterally - no improvised sub-auditor panels, no fuzzing campaigns, no dimensions beyond the Goal's list; the single convergence evaluator defined in the Definition of done is the only sub-agent review this Method authorizes. A case for more rigor is a Proposed item naming its cost; the user decides. Never file a finding inside a class recorded under Settled classes unless the code implementing that class changed after settlement. A finding a previous full audit scored clean, on code unchanged since, needs new evidence - a reproduced failure - not a deeper reading of the same lines.

## Constraints
- Do not change public interfaces or observable behavior without recording the rationale in JOURNAL.md.
- Do not weaken security, weaken or delete tests, or remove features to make a metric pass.
- Full implementations only. A placeholder or stub written to satisfy an acceptance check is a violation, not progress.
- A task is done only when its acceptance check has been run and observed to pass. Evidence over assertion, always.
- Prefer deletion and simplification over addition. No speculative abstractions, options, or features.
- Match the project's existing conventions. No wholesale reformatting or cosmetic churn.
- Prefer well-maintained, widely adopted libraries. No heavy dependencies for trivial problems.
- Keep each change atomic. The per-iteration checkpoint commit is the revert unit; never push, never create branches, and never rewrite checkpoint history.

## Lessons
Operational rules future iterations must obey, learned during runs. One line each: a build quirk, a command that must or must not be used, a mistake made twice. When a JOURNAL Learnings line states a rule future iterations need, copy it here as one line. The project owner can add lines here too, to steer every future run: fix the loop, not the run. Keep it brief; never write status reports, run narration, or task state here. A Lesson recording its second occurrence is marked `[recurred]`, and the run report proposes promoting it to a mechanism - a hook check or a Method rule - for the user to decide: a rule that had to be written twice is a rule this text is not enforcing.

- Read the `CREATE TABLE` before writing a probe that inserts rows; guessing columns costs a run, and on Windows the failed connection holds the temp file open so the real error is buried under a tempdir PermissionError.
- Sweep a retry or backoff policy by counting attempts and sleeps, never by watching a call succeed: "it worked" looks identical whether the policy retried three times, once, or not at all.
- The privacy standing order in CLAUDE.md outranks everything here: probes, fixtures, docs and commit messages use invented titles from docs/TEST-DATA.md, never real library items. `leak_check.py` is part of the Verify command, so a breach fails the gate.
- The leak gate matches WHOLE WORDS as of 570d5f4, which ended the class of trip that could not be reworded: a term buried inside a longer identifier - a standard library class name, a builtin exception name - no longer hits. Eleven ALLOWED entries that existed only to excuse such collisions were removed with it. What remains is the whole-word collision, where an ordinary English word is also a catalog term; that still needs a reword, and widening ALLOWED is still the wrong remedy because it blinds the gate to every real title containing that word.
- `[recurred]` Prefer plain technical wording in probe and state files: an ordinary English noun can still collide with a private-library term as a whole word. Tripped 5 times on prose alone under the old substring rule, never on a real leak. Run the gate BEFORE the checkpoint rather than at the end of the iteration, so a trip costs a reword instead of a reverted commit.
- A probe's fixture must not read a constant the unfixed code lacks: the helper raises, the case passes for the wrong reason, and the differential run silently measures nothing. Hold such a value as a literal and assert it equals the shipped constant separately.
- A class is enumerated by listing every site of the IDIOM, never by grepping for the fix: a grep that matches only already-fixed sites is structurally blind to the unfixed ones and will certify an incomplete class as settled.
- Genre names are private data too, not neutral vocabulary: a plausible one used as a probe fixture tripped the gate in iteration 3. Invent an obviously fake one rather than widening `ALLOWED`.
- Probe a network guard with real `http.server` instances on ephemeral ports, never a mocked session: a mock proves only what it was written to do.
- Never branch on `requests`' `is_redirect` (or any truthiness) in code the suite drives with `Mock` responses - every Mock attribute is truthy. Compare `status_code` against an explicit set.
- A battery asserts the DESIRED answer, never the observed one. A case written to match current behaviour certifies a defect as correct forever.
- A probe that opens a database uses a FRESH file per case. Reusing one carries the previous case's rows into the next and trips the `machine_name` UNIQUE constraint.
- `bool` is a subclass of `int` in Python: a range check on a JSON number must exclude it explicitly, or `true` passes as 1.
- Never pipe a test run or the Verify command through `Select-Object -Last N` in PowerShell: like `head`/`tail` in bash it reports the pipeline's status, not the suite's, so a red suite reads as green. Redirect to a file and check the exit status.
- The `[~]` inventory marker means unreachable on this host and nothing else. A row that is only partly swept splits into two rows; it never gets `[~]`.
- `.venv/Scripts/pip.exe` exits 1 silently in this project; use `.venv/Scripts/python.exe -m pip` instead.
- `[recurred]` A differential run needs the fix reverted and NOTHING else. Reverting a module that also gained a new name - an accessor, an exception class - breaks the import, and pytest reports a collection error, which looks like a red run and measures nothing. When the fix introduces a name the new tests import, the differential belongs in a probe battery that does not name it; run that battery before editing and record its score.
- Assert a close DIRECTLY - the connection object must raise on reuse - never "a second open still works". The weaker form only fails when a write was still pending, so on any path holding no lock it passes against the very defect it was written for.
- A round-trip invariant over a value that was rounded needs a tolerance of half the rounding step, or it fails on correct code: 4667 parsed to two places gives 4.67, and multiplying back gives 4670, not 4667.
- Run an enumerating check and read its real output BEFORE writing what it returns into a Settled classes line. `[recurred]` It happened again the next iteration, and the miss was caused by a safe-LOOKING spelling: `get_json(silent=True) or {}` reads as guarded and is not, so a grep for the unguarded form skipped six broken routes. Enumerate by driving the real registry - `app.url_map` - not by grepping for a pattern a variant can dodge. One was drafted from the two sites a finding happened to name, claiming 6; the grep returned 19 and 13 of them were still broken. A settled-class line is a claim about a whole class, and enumerating from memory is how an incomplete class gets certified as closed.
- A battery that derives its inputs by SCANNING needs a guard against passing vacuously: the selector check would find nothing, compare nothing and report clean if its regex ever stopped matching the codebase's style. Assert the scan found a plausible number of things.
- When a probe cannot observe half of what a row does, extend the harness rather than narrowing the assertion - but only after checking nothing depended on the old behaviour, and only with every battery sharing that harness re-run.
- A checker's own COVERAGE rules deserve a battery more than its matching rules: a gate that quietly skips a directory reports `clean` in the same words as one that read everything. Probe the exclusions on both sides.
- A probe that drives a CLI must never invoke a subcommand whose SUCCESS writes: `export`'s path is optional, so a happy-path case wrote 600 KB of the owner's catalog into the repo root, untracked and one `git add -A` from being committed. `check_no_data_tracked` filters TRACKED paths and `leak_check` scans only what is staged, so neither would have caught it first. Assert a happy path in-process, from `--help`, or not at all.
- Where one input shape is produced by two different normal states, the refusal IS the feature: assert that the stored data survived it, not merely that the call raised.
- When a first assertion fails, check whether the SECOND encodes the same misunderstanding: two attempts at one case were both wrong models of a width cap, and only reading the rendered output settled it. A failing assertion is evidence about the model, not only about the value.
- For a destructive operation, assert the BYTES rather than the return value: a function can report refusal and still have written, and on a restore surface that difference is the whole point.
- A stub's default must sit on the LIVE side of any freshness check it feeds: a stub source defaulting to a fixed past reset time made the quota assertions test nothing, because the code correctly declines to believe an expired record. Fixture defaults are relative to now, or they expire.
- State a row's scope in its battery when a neighbouring row owns part of the surface, and say what a stub stands in for: two rows here reach the network but their guards are swept elsewhere, so a real server would re-test the refusal and never reach the mapping the row certifies.
- When a probe crashes the code under test, decide whether the INPUT was in envelope before calling it a finding: an unguarded spread really did raise, and the right answer was still to fix the fixture, because asserting a shape the server cannot emit pins a contract the project never made.
- Write a battery's expected ORDER from the property that orders it, never from the order the fixture is written in: five cases asserted id order while the code sorts by name, and the failures pointed at the sort, which was correct.
- A documented score band or formula is a known-answer surface: derive every expected value from it and assert the constants in the same battery, and assert the ORDERING between bands separately - one tier's numbers can be right while the ranking the tiers exist to provide is wrong.
- When a shared helper is the right remedy but one of its callers serves a second audience, fix at the boundary the finding is about, not in the helper: `db.normalize_tags` coercing with `str()` is correct for the import paths and wrong for the viewer, so the check went at the viewer and left a swept row unstaled.
- A probe helper that FALLS BACK when it cannot find what it is looking for reads a neighbouring field and asserts against it: a title lookup defaulting to column index 1 made eight cases compare titles against `ebook` and blamed the routes. A locator in a battery asserts its target exists.
- Sweep a scoring cutoff by DERIVING the expected verdict from the documented constant, never by feeding real-looking data and recording what comes back. Synthetic inputs whose score is computable by hand (`fuzz.ratio` is 2*M/T over letter strings) turn a fuzzy matcher into a known-answer surface; assert the constants in the same battery so the derivation fails loudly if they move.
- `x or {}` is not a type check, and a probe written with EMPTY off-shape values certifies the guard it was written to test: `[] or {}` is `{}`, so only a NON-EMPTY list reaches the attribute access and raises. Use non-empty off-shape values.
- A probe of a keyed source clears that provider's API-key environment variable before importing it. Every keyed source falls back to `os.environ` when no key is passed, so on a host where the owner has exported a real key the "source is unconfigured" cases send a real request and assert the opposite of what they claim; this passed spuriously once before the variables were cleared.
- Ordinary English prose in PLAN.md, BACKLOG.md or JOURNAL.md can trip the gate with no private data present, because a catalog term can be an ordinary word. The Jeffy template's own Definition-of-done wording tripped it on one such noun in iteration 1 of the previous run. Reword the prose; do not add a generic word to `ALLOWED`, which would blunt the gate for the whole repo. Do not quote the offending word in the Lesson either - that just moves the hit.
- A settled class is only as wide as its enumerating check. A1 settled unvalidated viewer request bodies by grepping direct-index reads, which was structurally blind to the two POST routes that read the body with `.get`; both answer 500 on a non-object body. When settling a class, check that the enumeration lists the IDIOM in every spelling the codebase actually uses.

## Definition of done
Convergence requires a full audit pass, executed in one iteration, that rescores every applicable dimension against the severity rubric and the Operating envelope with fresh evidence and finds zero High and zero Medium in-envelope findings, requires that the Surface inventory lists no unswept row - a converged project is one whose whole mapped surface has actually been examined, enforced mechanically by the Stop hook - and it leaves no open task behind: at convergence the Now, Next, and Later sections of BACKLOG.md are empty, because every filed finding, Low included, has been completed, moved to Declined with a genuine not-worth-fixing reason, or blocked with its reason recorded and named in the run report. A converged project is completely done, not done except the small things. A `- [~]` row - surface this host cannot reach, carrying its reason - is not an unswept row and does not block convergence, but the run report names every one of them. When the closing audit itself files new findings the run is not yet converged: execute them in the following iterations and declare convergence once the backlog is empty again, provided the only commits since that clean audit are the fixes for tasks it or the evaluator gate filed plus loop state edits and the Verify command exits 0 in the declaring iteration with its output recorded in the JOURNAL entry; if anything else changed, audit again. Proposed items never block convergence. Partial or replenishment audits never count toward convergence. Convergence also requires the adversarial evaluator gate: exactly one fresh-context sub-agent, spawned carrying none of the run's context, re-runs the Verify command and the closed tasks' acceptance checks, hunts for missed in-envelope High or Medium findings in the run's changes, and must return PASS, recorded in the JOURNAL entry of the iteration that ran it; a PASS that does not declare in the same iteration does not carry forward, and the declaring iteration re-invokes the gate so its closing entry records the verdict the Stop hook reads there; a REJECT files its evidenced reasons as ordinary tasks and the run continues, at most 2 evaluator invocations per run and 3 when the first landed before the midpoint of the budget, a second REJECT ends the run as a hard blocker, a cap reached without a PASS in the closing entry ends the run the same way because no invocation remains to produce the verdict declaring requires, and when sub-agent spawning is unavailable the run records Evaluator: unavailable with the reason and may converge under the remaining conditions. The gate is not only a closing ceremony: when the ledger first empties with a clean full audit already recorded this run and at least 3 iterations remaining, run it that iteration rather than at the declaration, because a REJECT files tasks and answering them needs budget the last iteration no longer has. The ratchet never invokes the evaluator. Severity is set by the rubric and the envelope alone: downgrading a finding to reach convergence is a violation, and Declined items must be genuinely not worth fixing, not deferred work. Pricing is how not worth fixing is judged: a Low whose class is not runtime and whose fix plus its regression test does not fit in one iteration goes to Declined with the reason `cost: exceeds one iteration`, named by ID in the run report - a Declined reason, never a lower severity. Every run ends with a run report to the user - converged, out of budget, or blocked - listing iterations used, tasks closed with severities, the run's diffstat, blocked and Proposed items, and the final Verify status. Record the closing scores and their evidence in JOURNAL.md, and, when the project is a git repository, append a line reading Converged: <full commit hash> - <date> under the ## Converged section of BACKLOG.md, before claiming convergence.

Convergence ratchet: when the latest Converged line in BACKLOG.md names a commit, no open task remains in Now, Next, or Later, the run carries no focus directive, and nothing but loop state has changed since that commit - every path in git status --porcelain and in git diff --name-only against that hash is one of PLAN.md, BACKLOG.md, JOURNAL.md, JOURNAL-archive.md, or a path under `.jeffy/`, which holds the probe batteries and is loop memory exactly as the ledger files are - verify exactly that and re-declare convergence immediately, without a fresh audit. A seeded backlog or a focus directive always gets a real audit or execution, never a ratchet. When code has changed, audit normally, but a finding in code unchanged since the convergence commit must be a reproduced in-envelope High or a regression traced to the new changes; anything else there is a Proposed item. In a project without git the ratchet degrades to the Settled classes rule alone. The iteration budget is the hard stop.
